mutation UserLogin($input: UserLoginInput!) {
  userLogin(input: $input) {
    data {
      accessToken
      refreshToken
      user {
        role
        name
        id
        email
        avatar
        familyId
        templeIds
      }
    }
    errorCode
    errorMessage
    statusCode
    timestamp
  }
}
