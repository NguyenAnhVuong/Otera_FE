mutation UserLogin($input: UserLoginInput!) {
  userLogin(input: $input) {
    data {
      accessToken
      user {
        role
        name
        id
        email
        avatar
      }
    }
    errorCode
    errorMessage
    statusCode
    timestamp
  }
}
