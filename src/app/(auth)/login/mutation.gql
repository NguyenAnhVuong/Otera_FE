mutation UserLogin($input: UserLoginInput!) {
  userLogin(input: $input) {
    data {
      accessToken
      user {
        role
        name
        id
        email
        avatar
        familyId
      }
    }
    errorCode
    errorMessage
    statusCode
    timestamp
  }
}
