mutation UserRegister($input: UserRegisterInput!) {
  userRegister(input: $input) {
    timestamp
    statusCode
    errorMessage
    errorCode
    data {
      birthday
      email
      familyId
      id
      name
      refreshToken
      role
      createdAt
    }
  }
}
