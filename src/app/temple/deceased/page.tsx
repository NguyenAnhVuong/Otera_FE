import TempleDeceasedList from "@/components/Templates/TempleDeceasedList";
import { NextPage } from "next";
import React from "react";

type PageProps = {};

const Page: NextPage<PageProps> = ({}) => {
  return <TempleDeceasedList />;
};

export default Page;
